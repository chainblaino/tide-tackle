# Firebase admin credentials — NEVER commit these
tools/serviceAccount.json
serviceAccount.json
*.serviceAccount.json

# Node
node_modules/
package-lock.json

# macOS
.DS_Store
**/.DS_Store

# Local Clancy tuning/authoring tools — never ship these
clancy-guide.html
clancy-tuner.html
